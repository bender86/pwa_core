@* App.razor *@
@using PWA.Auth.Components
@inject IJSRuntime JSRuntime

<CascadingAuthenticationState>
    <Router AppAssembly="@typeof(App).Assembly">
        <Found Context="routeData">
            <AuthorizeRouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)">
                <NotAuthorized>
                    @if (context.User.Identity?.IsAuthenticated != true)
                    {
                        <App.RedirectToLogin />
                    }
                    else
                    {
                        <p role="alert">Vous n'etes pas autorisé à accéder à cette ressource.</p>
                    }
                </NotAuthorized>
            </AuthorizeRouteView>
            <FocusOnNavigate RouteData="@routeData" Selector="h1" />
        </Found>
        <NotFound>
            <PageTitle>Non trouvé</PageTitle>
            <LayoutView Layout="@typeof(MainLayout)">
                <p role="alert">D�solé, cette page n'existe pas.</p>
            </LayoutView>
        </NotFound>
    </Router>
</CascadingAuthenticationState>

@code {
    protected override void OnInitialized()
    {
        JSInterop.Initialize(JSRuntime);
    }

    public class RedirectToLogin : ComponentBase
    {
        [Inject] private NavigationManager Navigation { get; set; } = null!;
        
        protected override void OnInitialized()
        {
            Navigation.NavigateTo("/login", replace: true);
        }
    }
}
<style>
        /* ... votre CSS existant ... */

        /* Fix z-index pour les dialogs MudBlazor */
        .mud-overlay {
            z-index: 2000 !important;
        }

        .mud-dialog-container {
            z-index: 2001 !important;
        }

        .mud-dialog {
            z-index: 2002 !important;
        }

        /* Carousel doit rester en dessous */
        .mud-carousel {
            z-index: 1 !important;
        }
    </style>