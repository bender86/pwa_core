{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "🚀 Launch PWA Auth (HTTP + Watch)",
            "type": "blazorwasm",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "browser": "edge",
            "url": "http://localhost:5074",
            "webRoot": "${workspaceFolder}/wwwroot",
            "timeout": 120000,
            "preLaunchTask": "watch-run"
        }
    ]
}